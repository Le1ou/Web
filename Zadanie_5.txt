Ќаписать функцию, принимающую 1 аргумент Ч число от 0 до 100000, и
возвращающую true, если оно простое, false если нет

def IsPrime(value):
    divider = 2
    while value % divider != 0:
        divider += 1
    return divider == value
value = int(input("¬ведите число: "))
if(value in range(0, 100001)):
    result = IsPrime(value)
    print(result)
else:
    print("„исло вне дипазона")